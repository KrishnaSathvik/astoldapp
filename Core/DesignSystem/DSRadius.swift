import CoreGraphics

/// Corner radii — used only where controls/sheets need shape. See docs/03-design-system.md §8.
enum DSRadius {
    static let small: CGFloat = 8
    static let medium: CGFloat = 12
    static let large: CGFloat = 18
    static let xl: CGFloat = 24
}
