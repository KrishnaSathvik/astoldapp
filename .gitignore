# Node — the website has its own node_modules and .gitignore; a stray one at the root is never source
node_modules/

# Xcode / generated project
Yourly.xcodeproj/
*.xcworkspace
!default.xcworkspace
xcuserdata/
DerivedData/
build/
.build/

# macOS
.DS_Store

# Swift Package Manager
Package.resolved
.swiftpm/

# Secrets — never commit real keys
.env
.env.*
!.env.example

# Misc
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Vercel local link (project/org ids)
.vercel/
.playwright-mcp/

# Python bytecode (docs/appstore/compose.py)
__pycache__/
*.pyc
